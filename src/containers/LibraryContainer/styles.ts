import styled from "styled-components";
import { Flex, Text as ChakraText } from "@chakra-ui/react";

export const Container = styled(Flex)``;

export const Body = styled(Flex)``;

export const DropsWrapper = styled(Flex)``;
