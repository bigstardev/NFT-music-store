import styled from "styled-components";
import { Flex, Text as ChakraText } from "@chakra-ui/react";

export const Container = styled(Flex)``;

export const LeftColumn = styled(Flex)``;

export const LeftTitle = styled(Flex)``;

export const LeftSubtitle = styled(Flex)``;

export const LogoIcon = styled.img`
  height: 31px;
`;

export const RightColumn = styled(Flex)``;

export const BackIcon = styled.div`
  cursor: pointer;
`;

export const CloseIcon = styled.div`
  cursor: pointer;
`;

export const RightTitle = styled(ChakraText)``;

export const Tip = styled(ChakraText)``;

export const Img = styled.img``;

export const Regular = styled(ChakraText)``;

export const ErrorMessage = styled(ChakraText)``;
