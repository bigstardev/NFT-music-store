import styled from "styled-components";
import { Flex, Box, Text as ChakraText } from "@chakra-ui/react";

export const Container = styled(Flex)``;

export const CardContainer = styled(Flex)``;

export const ImageBox = styled(Flex)``;

export const ImageRelative = styled(Box)``;

export const Image = styled.img`
    width: 100%;
    aspect-ratio: 1;
`;

export const PositionWrapper = styled(Flex)``;

export const Position = styled(Flex)``;

export const Username = styled(ChakraText)``;

export const Money = styled(ChakraText)``;
