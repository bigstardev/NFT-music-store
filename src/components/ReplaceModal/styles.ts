import styled from "styled-components";
import { Flex, Box, Text as ChakraText } from "@chakra-ui/react";

export const Container = styled(Flex)``;

export const SongContainer = styled(Box)``;

export const SongInner = styled(Flex)``;

export const ImageContainer = styled(Box)``;

export const ReplaceContainer = styled(Flex)``;

export const SongTitle = styled(ChakraText)``;

export const SongArtist = styled(ChakraText)``;

export const Title = styled(ChakraText)``;

export const Subtitle = styled(ChakraText)``;
