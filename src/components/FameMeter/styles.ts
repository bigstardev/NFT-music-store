import styled from "styled-components";
import { Flex, Box, Text } from "@chakra-ui/react";

export const Container = styled(Box)``;

export const BarContainer = styled(Box)``;

export const BarFiller = styled(Flex)`
    transition: width 0.2s ease-in;
`;

export const FameContainer = styled(Flex)``;

export const TextWrapper = styled(Flex)``;

export const MeterText = styled(Text)``;

export const DecayWrapper = styled(Flex)``;
export const DecayImg = styled.img``;
export const DecayText = styled(Text)``;
