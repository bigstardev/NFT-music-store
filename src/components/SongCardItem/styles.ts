import styled from "styled-components";
import { Flex, Text as ChakraText } from "@chakra-ui/react";

export const CardContainer = styled(Flex)``;

export const ImageBox = styled(Flex)``;

export const Image = styled.img`
    width: 100%;
    aspect-ratio: 1;
`;

export const PositionWrapper = styled(Flex)``;

export const Position = styled(Flex)``;

export const SongName = styled(ChakraText)``;

export const Artist = styled(ChakraText)``;

export const DotContainer = styled(Flex)``;

export const Dot = styled(Flex)``;

export const FocusContainer = styled(Flex)``;
