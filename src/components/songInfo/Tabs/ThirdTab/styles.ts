import styled from "styled-components";
import { Flex, Box, Text as ChakraText } from "@chakra-ui/react";

export const Container = styled(Flex)``;

export const CardContainer = styled(Flex)``;

export const UpperCont = styled(Box)``;

export const BottonCont = styled(Flex)``;

export const Title = styled(ChakraText)``;

export const Subtitle = styled(ChakraText)``;

export const EllipsisButton = styled(Box)``;

export const ImageCont = styled(Flex)``;
