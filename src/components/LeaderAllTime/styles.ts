import styled from "styled-components";
import { Flex, Box, Text as ChakraText } from "@chakra-ui/react";

export const Container = styled(Flex)``;

export const AllTime = styled(ChakraText)``;

export const Image = styled.img`
    width: 180px;
    aspect-ratio: 1;
    margin: 0 auto;
`;

export const Username = styled(ChakraText)``;

export const Record = styled(Box)``;

export const RowWrapper = styled(Flex)``;

export const ColumnWrapper = styled(Flex)``;

export const Number = styled(ChakraText)``;

export const Label = styled(ChakraText)``;
