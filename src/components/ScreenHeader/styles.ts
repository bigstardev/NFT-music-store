import styled from "styled-components";
import { Flex, Box, Text } from "@chakra-ui/react";

export const Container = styled(Flex)``;

export const TitleWrapper = styled(Flex)``;

export const BackBtn = styled(Box)``;

export const HeaderTitle = styled(Text)``;

// HEADER DROPDOWN
export const DropdownContainer = styled(Flex)``;

export const ImageCont = styled(Flex)``;

export const Dot = styled(Box)``;

export const DropdownText = styled(Text)``;
export const Logout = styled(Box)``;
export const WalletInfor = styled(Flex)``;
