import styled from "styled-components";
import { Flex, Text as ChakraText, Input as ChakraInput } from "@chakra-ui/react";

export const Container = styled(Flex)``;

export const Label = styled(ChakraText)``;

export const Input = styled(ChakraInput)``;
