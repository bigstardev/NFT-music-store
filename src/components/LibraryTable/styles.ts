import styled from "styled-components";
import { Flex, Box, Text } from "@chakra-ui/react";
import Image from "next/image";

export const Container = styled(Flex)``;

export const RowWrapper = styled(Flex)``;

// SONG COLLUMN
export const SongColumnWrapper = styled(Flex)``;

export const SongImage = styled(Image)`
    border-radius: 4px;
`;

export const SongInfoWrapper = styled(Flex)``;

export const SongTitle = styled(Text)``;

export const SongArtist = styled(Text)``;

// GENERAL COLUMN
export const ColumnWrapper = styled(Flex)``;

export const Wrapper = styled(Flex)``;

export const InfoText = styled(Text)``;

export const LabelText = styled(Text)``;

export const Divider = styled(Box)``;

export const EllipsisButton = styled(Box)``;
