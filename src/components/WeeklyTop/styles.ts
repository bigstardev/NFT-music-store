import styled from "styled-components";
import { Flex, Box, Text as ChakraText } from "@chakra-ui/react";

export const Container = styled(Flex)``;

export const Header = styled(Flex)``;

export const HeaderTitle = styled(ChakraText)``;

export const ItensWrapper = styled(Flex)``;

// ITEM
export const LeaderContainer = styled(Flex)``;

export const LeftWrapper = styled(Flex)``;

export const Position = styled(Flex)``;

export const UserImg = styled.img``;

export const UserWrapper = styled(Flex)``;

export const Username = styled(ChakraText)``;

export const Sub = styled(ChakraText)``;

export const Points = styled(ChakraText)``;
